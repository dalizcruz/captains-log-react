import { Link } from "react-router-dom";

const Logs = ({ 
    // "PROPS (CAPTAIN'S LOG) FROM APP.JS" 
}) => {
    return(
        <div>
            {/* "MAP THROUGH LOGS, CREATE LIST AND LINK EACH LI TO LOGS/:INDEX" */}
        </div>
    )
}
export default Logs;